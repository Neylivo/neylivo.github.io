# dist/ — рабочий каталог сборки. На сайт уезжает не он, а корень ветки:
# собранное раскладывает туда publish.mjs, потому что иначе выкладку перебивает
# служебная сборка Jekyll от GitHub. Подробности — в publish.mjs.
dist/
dist-look/
node_modules/
